<!-- tcrn-doc-synced-to: CONTRIBUTING.md bc60c3bff58804345ea57120563308e5e3512a83a84ac749c004622a980d21f3 -->

> **英語版が正本です。** この翻訳は便宜のために提供されるものであり、両者が異なる場合は [CONTRIBUTING.md](./CONTRIBUTING.md) にある英語のテキストが優先されます。

[English](./CONTRIBUTING.md) · [简体中文](./CONTRIBUTING.zh-CN.md) · 日本語 · [한국어](./CONTRIBUTING.ko.md) · [Français](./CONTRIBUTING.fr.md)

# コントリビューション

このリポジトリは、見知らぬ人が自分の手で検証することを期待された一つのファイルと、TCRN Workflow を駆動する方法をエージェントに教える Skill ペイロードを配布します。以下のほとんどの規則はそこから導かれます:出荷されているのはバイトについての主張であり、**その主張を確かめにくくする変更は、コードを綺麗にするものであっても後退です**。

## 変更を提案する前に

`pnpm test` を完全に実行してください。部分実行は不可、`pnpm verify` での代替も不可です——両者は異なる問いに答えます。かつてタグ付きのコミットが 72 件中 22 件を赤にしたまま出荷されたのは、まさに `verify` を代わりに実行したためでした。`verify` は成果物がそのダイジェストと一致することを確認しますが、ピン留めされた identity と provenance が一致するかどうかについては何も述べません。

`bootstrap/trusted-bootstrap.mjs`、`IDENTITY`、いずれかの `EXPECTED_*` 定数、または `manifests/` 配下に触れる変更は、コミット**前**に全体スイートを実行し、コミット**後**に `pnpm push-gate` を実行しなければなりません。そのゲートはコミット済みのツリーを judge するため、先に実行すると自分の未コミットのバイトが報告されるだけです。

## 例外のない三つの規則

**ランタイム依存ゼロ。** bootstrap は一つのファイルであり、一つのままです。依存を加えることは、ダイジェストを一つ検証した見知らぬ人に、ここからは見えないサプライチェーンを信頼させることを意味します。

**検証経路にネットワークはない。** 検証は構造上オフラインです。判断のためにネットワークを要するチェックは、利用者が再現できるチェックではありません。

**アンカーのダイジェストは六か所に公開されるか、公開されていないかのどちらかです。** bootstrap のバイトを変える手順は、利用者が手で確かめる値を変えます。六つの文書のいずれか一つでアンカーが古ければ、その読者には「ダウンロードが改竄された」と伝わります。`push-gate` が欠落したアンカーと置き換えられたアンカーの**両方**で失敗するのは、まさにそのためです。

## リリース

`RELEASING.md` が儀式そのものであり、その順序は装飾ではありません:スイートは `artifacts/` を書き換えるため、candidate manifest はその**後**に束ねられます。ソースアーカイブはアンカーが確定した**後**に構築されます——それがアンカーを載せる文書群を含むからです。再構成せず、読んでください。

provenance ファイルは Workflow リポジトリの生成物の**バイトコピー**です。手で書かないでください。そして更新を拒まないでください:生成器は存在し、それはもう一方のリポジトリにあります。

## 翻訳

英語の文書が規範的な正本です。各翻訳は `tcrn-doc-synced-to` ピンを持ち、その元ファイルと元の SHA-256 を示します。英語文書への構造的変更は**同じ変更の中で**ミラーされます——黙って元より遅れている便宜的な翻訳は、存在しない翻訳より悪いのです。現在のものとして読まれてしまうからです。

## 良い報告とは

検証の失敗について:バージョン、算出した値、文書が期待すると告げた値、そしてファイルの入手元。それ以外について:期待したこと、起きたこと、そして正確なコマンド。**reason code は、エラーメッセージの説明よりも価値があります。**
