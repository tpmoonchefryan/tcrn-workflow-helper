<!-- tcrn-doc-synced-to: CONTRIBUTING.md bc60c3bff58804345ea57120563308e5e3512a83a84ac749c004622a980d21f3 -->

> **英文版为权威版本。** 本翻译仅供参考;如有出入,以 [CONTRIBUTING.md](./CONTRIBUTING.md) 的英文文本为准。

[English](./CONTRIBUTING.md) · 简体中文 · [日本語](./CONTRIBUTING.ja.md) · [한국어](./CONTRIBUTING.ko.md) · [Français](./CONTRIBUTING.fr.md)

# 参与贡献

本仓分发一个期望由陌生人亲手校验的文件,以及一份教 agent 驱动 TCRN Workflow 的 Skill 载荷。下面几乎每一条规则都由此而来:被分发的东西是一个关于字节的主张,所以**一个让该主张更难被核对的改动就是退步**,哪怕它让代码更好看。

## 提出改动之前

完整跑 `pnpm test`。不是子集,也不能用 `pnpm verify` 代替——两者回答的是不同的问题,曾有一个打了标签的提交带着 72 项测试里的 22 项红色发出去,原因正是用 `verify` 代跑。`verify` 校验产物与其摘要相符;它对「钉定的身份与 provenance 是否一致」只字不提。

任何触及 `bootstrap/trusted-bootstrap.mjs`、`IDENTITY`、某个 `EXPECTED_*` 常量,或 `manifests/` 下任何内容的改动,提交**之前**必须完整跑套件,提交**之后**跑 `pnpm push-gate`——那道门判的是已提交的树,提前跑只会把你自己未提交的字节报给你看。

## 三条没有例外的规则

**零运行时依赖。** bootstrap 是一个文件,并且保持是一个文件。引入依赖意味着:校验了一个摘要的陌生人,正在信任一条他从这里看不见的供应链。

**校验路径上没有网络。** 校验在构造上就是离线的。如果一项检查需要网络才能下判断,那它就不是用户能够重复的检查。

**锚点摘要发布在六处,否则就不算发布。** 改变 bootstrap 字节的步骤,同时改变了用户亲手核对的那个值;六份文档里任意一份的锚点陈旧,都会告诉那位读者「下载被篡改了」。`push-gate` 对缺失的锚点和被取代的锚点**都**判红,正是为此。

## 发版

`RELEASING.md` 是仪式本身,按顺序,而这个顺序不是装饰:套件会重写 `artifacts/`,所以候选清单在它**之后**绑定而不是之前;源归档在锚点最终确定**之后**构建,因为它覆盖承载锚点的那些文档。请读它,而不是重新推导它。

provenance 文件是 Workflow 仓生成物的**字节复制**。不要手写它,也不要拒绝更新它:生成器是存在的,它在另一个仓里。

## 翻译

英文文档是规范正本。每份译文携带一个 `tcrn-doc-synced-to` 钉,写明它的源文件与该源的 SHA-256;对英文文档的结构性改动必须在**同一次改动**里镜像——一份无声落后于源头的便利译文,比不存在的译文更糟,因为它读起来像是当前的。

## 一份好的报告长什么样

对于校验失败:版本、你算出的值、文档告诉你应当得到的值,以及你从哪里取得该文件。对于其它情况:你预期什么、实际发生了什么、以及确切的命令。**一个 reason code 比一段对错误信息的描述更有价值。**
