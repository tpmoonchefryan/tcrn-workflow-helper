<!-- tcrn-doc-synced-to: CONTRIBUTING.md bc60c3bff58804345ea57120563308e5e3512a83a84ac749c004622a980d21f3 -->

> **영어가 권위 있는 버전입니다.** 이 번역은 편의를 위해 제공됩니다. 둘이 다를 경우 [CONTRIBUTING.md](./CONTRIBUTING.md) 의 영어 텍스트가 우선합니다.

[English](./CONTRIBUTING.md) · [简体中文](./CONTRIBUTING.zh-CN.md) · [日本語](./CONTRIBUTING.ja.md) · 한국어 · [Français](./CONTRIBUTING.fr.md)

# 기여 안내

이 저장소는 낯선 사람이 직접 검증하도록 기대되는 파일 하나와, 에이전트에게 TCRN Workflow를 운전하는 법을 가르치는 Skill 페이로드를 배포합니다. 아래 규칙 대부분이 거기서 따라 나옵니다: 배포되는 것은 바이트에 관한 주장이므로, **그 주장을 확인하기 어렵게 만드는 변경은 코드를 더 보기 좋게 만들더라도 퇴보입니다**.

## 변경을 제안하기 전에

`pnpm test`를 전부 실행하십시오. 일부만도 안 되고, `pnpm verify`로 대체해서도 안 됩니다 — 둘은 서로 다른 질문에 답합니다. 태그가 붙은 커밋이 72개 중 22개를 붉은 채로 출하된 적이 있는데, 원인이 바로 `verify`를 대신 실행한 것이었습니다. `verify`는 산출물이 그 다이제스트와 맞는지 확인할 뿐, 고정된 identity와 provenance가 일치하는지에 대해서는 아무 말도 하지 않습니다.

`bootstrap/trusted-bootstrap.mjs`, `IDENTITY`, `EXPECTED_*` 상수, 또는 `manifests/` 아래 무엇이든 건드리는 변경은 커밋 **전에** 전체 스위트를, 커밋 **후에** `pnpm push-gate`를 실행해야 합니다. 그 게이트는 커밋된 트리를 판정하므로, 먼저 실행하면 자신의 커밋되지 않은 바이트를 되돌려 보여줄 뿐입니다.

## 예외가 없는 세 가지 규칙

**런타임 의존성 0.** bootstrap은 파일 하나이고, 하나로 남습니다. 의존성을 더한다는 것은, 다이제스트 하나를 검증한 낯선 사람에게 여기서는 보이지 않는 공급망을 신뢰하게 만드는 일입니다.

**검증 경로에 네트워크는 없습니다.** 검증은 구조적으로 오프라인입니다. 판단하는 데 네트워크가 필요한 검사라면, 그것은 사용자가 반복할 수 있는 검사가 아닙니다.

**앵커 다이제스트는 여섯 곳에 게시되거나, 게시되지 않은 것입니다.** bootstrap의 바이트를 바꾸는 단계는 사용자가 손으로 확인하는 값을 바꿉니다. 여섯 문서 중 하나라도 앵커가 낡았다면 그 독자에게는 "내려받은 것이 변조되었다"고 말하는 셈입니다. `push-gate`가 없는 앵커와 대체된 앵커 **둘 다**에서 실패하는 이유가 정확히 그것입니다.

## 릴리스

`RELEASING.md`가 의식 그 자체이며, 그 순서는 장식이 아닙니다: 스위트가 `artifacts/`를 다시 쓰므로 candidate manifest는 그 **뒤에** 묶이고, 소스 아카이브는 앵커가 최종 확정된 **뒤에** 만들어집니다 — 앵커를 담은 문서들을 포함하기 때문입니다. 재구성하지 말고 읽으십시오.

provenance 파일은 Workflow 저장소 생성물의 **바이트 복사본**입니다. 손으로 쓰지 마십시오. 그리고 갱신을 거부하지도 마십시오: 생성기는 존재하며, 다른 저장소에 있습니다.

## 번역

영어 문서가 규범적 정본입니다. 각 번역은 원본 파일명과 그 SHA-256을 담은 `tcrn-doc-synced-to` 핀을 지니며, 영어 문서의 구조적 변경은 **같은 변경 안에서** 반영됩니다 — 조용히 원본보다 뒤처진 편의용 번역은 없는 번역보다 나쁩니다. 현재의 것처럼 읽히기 때문입니다.

## 좋은 보고란

검증 실패의 경우: 버전, 계산한 값, 문서가 기대한다고 알려준 값, 그리고 파일을 얻은 곳. 그 밖의 경우: 무엇을 기대했는지, 무슨 일이 일어났는지, 그리고 정확한 명령. **reason code는 오류 메시지에 대한 설명보다 가치가 있습니다.**
